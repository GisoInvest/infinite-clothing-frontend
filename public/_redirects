# Netlify redirects for Single Page Application (SPA)
# This ensures all routes are handled by the React app

# API calls should go to the backend
/api/*  https://infinite-clothing-backend-dwea.onrender.com/api/:splat  200

# All other routes should serve index.html for client-side routing
/*    /index.html   200

